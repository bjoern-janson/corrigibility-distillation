# RG-001 Terminal Lesson

**Status:** `TERMINAL INTERPRETATION / NO REOPENING OF RG-001`  
**Scientific parent:** `RG001_REALIZER_ADMISSION_AUDIT_V0.1`  
**Date:** 2026-08-24

RG-001 is closed at realizer admission. This note preserves the program-level lesson without modifying the frozen semantic constitution, case family, admission contract, or admission audit.

## 1. Core localization

The terminal result supports the following distinction:

\[
\boxed{\text{functional resemblance}\not\Rightarrow\text{shared realizability at the chosen semantic resolution}.}
\]

The decisive matched pair remains:

\[
\mathcal Q_{04}(H)=\{\{G,w_a\}\}
\]

versus

\[
\mathcal Q_{05}(H)=\{\{G\},\{w_a\}\}.
\]

Both candidate realizers can carry the relevant ingredients and preserve the structural distinction in an encoding. Neither frozen native surface supplies the consequential alternative-sufficient-support semantics required by C05 without the adapter becoming an additional reasoning mechanism.

Thus RG-001 permanently separates:

\[
\boxed{\text{representation}\neq\text{operational semantics}}
\]

and maintains the further program-level distinction:

\[
\boxed{\text{operational semantics}\neq\text{causal consequence}.}
\]

The second distinction is a design boundary, not an empirical result of RG-001: RG-001 terminated before a valid common operational semantics was constituted, so the operational-semantics-to-causal-consequence transition was not tested.

## 2. Three-layer dependency ladder

Preserve the following notation for future realization-geometry work:

\[
\boxed{D_1=\text{representation of dependency}}
\]

\[
\boxed{D_2=\text{sufficient-support semantics}}
\]

\[
\boxed{D_3=\text{corrective consequence}}
\]

Program discipline:

\[
D_1\not\Rightarrow D_2,
\qquad
D_2\not\Rightarrow D_3.
\]

RG-001 directly localized failure at the first boundary:

\[
\boxed{D_1\rightarrow D_2\ \text{not constituted for either frozen candidate}.}
\]

It did **not** test the second boundary because `V_F` was never opened.

## 3. What RG-001 established

Under the frozen non-inventive adapter contract:

```text
SSI-CALC v0.1
  can encode/carry dependency structure
  does not natively provide arbitrary alternative-sufficient-support standing semantics

OpenCore Nano V0
  can encode/carry dependency structure
  natively provides conjunctive/transitive receipt liveness
  does not natively provide multi-provenance OR-of-receipts standing semantics
```

Therefore:

\[
\boxed{
A_{trans}^{RG}(SSI\!\mbox{-}CALC)=FAIL,
\qquad
A_{trans}^{RG}(Nano)=FAIL.
}
\]

Terminal classification:

```text
FUNCTION_EQUIVALENCE_NOT_CONSTITUTED
```

This is a realizer-admission null, not `V_F=0` and not a near-pass for Nano.

## 4. Hard closure rule

For RG-001 itself:

```text
RG-001                 TERMINAL
adapter repair         FORBIDDEN
semantic weakening     FORBIDDEN
same-realizer rescue   FORBIDDEN
RG-002 rescue attempt  FORBIDDEN
cost measurement       NOT RUN / FORBIDDEN
robustness measurement NOT RUN / FORBIDDEN
"Nano almost passed"   FORBIDDEN INTERPRETATION
```

Do not reopen RG-001 by adding a route aggregator, closure layer, alternative-support primitive, decoder rescue, or post-hoc semantic relaxation. Any such change defines a new scientific object and requires a new assay identifier.

## 5. Future realization-geometry design rule

The next meaningful realization-geometry experiment should begin from a function that all selected candidate realizers already natively inhabit.

\[
\boxed{\textbf{Find the common function first; do not manufacture the common function.}}
\]

Candidate selection must therefore precede coordinate comparison with an independently frozen native-inhabitation test. Only after common functional constitution may a future assay compare realization coordinates such as validity, cost, or robustness.

This is intentionally contrasted with the archived parity calibration:

```text
archived parity RG-002
  common function already constituted
  -> cost / robustness comparison admissible

main-line RG-001
  common function not constituted
  -> stop before comparison
```

The contrast is methodological, not evidence that parity is a preferred universal function.

## 6. Relation to RIL-003

RG-001 closure does not modify, accelerate, delay, or provide evidence for RIL-003.

RIL-003 remains on its own preregistered pre-reveal boundary until its frozen future-entropy protocol permits target materialization.

## 7. Permanent compression

> **Representing a dependency is not the same as possessing its support semantics, and possessing support semantics is not yet the same as producing the corrective consequence of interest.**

> **Find the common function first; do not manufacture the common function.**
