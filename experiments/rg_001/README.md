# RG-001 apparatus

Parallel realization-geometry assay. This directory is intentionally independent of `experiments/ril_003/`.

Frozen scientific object: 12-bit parity under two realizers (`LOOP12`, `LUT6`) sharing a 64-byte auxiliary-memory substrate.

Primary coordinates: exact validity, internal CPython opcode cost, and exact robustness under the preregistered 512 single-bit auxiliary-memory faults.

Do not import or inspect RIL-003 targets, Beacon data, or result artifacts from this apparatus.
