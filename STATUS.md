# Research Status

Last reconciled: **2026-08-24**

This file is the mutable current-state ledger. Frozen scientific artifacts retain their own historical anchors and must not be rewritten to match this summary.

## 1. Corpus Distillation — CLOSED

```text
canonical corpus                         14 repositories
Phase 1 inventory                        COMPLETE
Phase 2 directional substitutability     0 / 182 YES or PARTIAL
redundancy                               NOT EARNED
minimization                             CLOSED
L4 full mechanism interfaces             0
L4' bounded evidence/provenance handoff  1
L5 composition                           CLOSED
L6 cumulative gain                       CLOSED
```

Local necessity audit:

```text
NECESSARY          3
NOT DEMONSTRATED   5
DISCONFIRMED       1
```

Necessity-relation audit:

```text
DEPENDENT          0
INDEPENDENT        0
NOT COMPARABLE     2
NOT DEMONSTRATED   1
```

Frozen conclusion:

> Recurring corrective structure exists and some distinctions are locally indispensable, but no common reducible core was earned.

No downstream experiment is allowed to rewrite these counts or promote a later prospective interface into historical L4 evidence.

## 2. CGP-001 — CLOSED / NOT EVALUABLE

Question:

```text
Can frozen cross-corpus separation morphology prospectively predict a new relation?
```

Candidate corridor:

```text
NSS search allocation -> FS007 repair search
```

Frozen preregistration anchor:

```text
669a94aac6c07484323dde3b0fb64df5b9ec4bca
```

Recovered apparatus anchor from the interrupted local workspace:

```text
50f817ce584a18a68237340fe05ede34602b874a
```

Recovered translation-audit anchor:

```text
b5ee054628926de3509b7a6d7f50918dec319372
```

Terminal audit:

```text
A_trans                    FAIL
failed criteria            8, 9
CGP-001                     NOT EVALUABLE
primary arm execution       NOT AUTHORIZED / NOT RUN
L0-L3                       NOT REACHED
H_CG                        NOT TESTED
adapter repair in CGP-001   FORBIDDEN
```

Criterion 8:

```text
FS target/scoring output != NSS resolving-probe identity
```

Criterion 9:

```text
NSS per-signature expansion != global FS M1 repair-search opportunity
```

No B-code was assigned because the preregistration forbids boundary localization after `A_trans=FAIL`.

The null is preserved as a pre-composition typing/interface failure, not converted into evidence for or against the snowflake hypothesis.

## 3. RIL-001 — PREREGISTERED / NOT EXECUTED

Lane:

```text
REPRESENTATION-INDUCED LEVERAGE
```

Frozen preregistration anchor:

```text
204fe919159145ac9c29f1becfb92b0c511af02b
```

Scientific parent:

```text
Future Sufficiency Experiment 007
bjoern-janson/future-sufficiency@2f4ca824e02b89df0c23d64de312c4f93a4c8a41
```

Frozen function:

```text
F = low-cost NEEDS_FANOUT repair
```

Representations:

```text
R0_AST   canonical Program AST
R1_SEM8  exact 8-pattern semantic tuple already present as the parent dictionary key
```

Intervention:

```text
A fixed
F fixed
D fixed
Omega_req fixed
authority fixed
R varied
```

Primary leverage quantity:

```text
Lambda_F^op = C_R0^op(F) / C_R1^op(F)
```

but it is interpretable only after the entire preservation predicate `P=1` and the fixed-algorithm gate pass.

Current status:

```text
implementation             DOES NOT EXIST
execution manifest          DOES NOT EXIST
A_fixed audit               NOT RUN
preservation audit          NOT RUN
cost result                 DOES NOT EXIST
scientific outcome          NONE
```

Do not infer a positive or negative RIL result from the preregistration design.

## 4. Current conceptual separation

These are useful organizing distinctions, not established universal theory:

```text
Constraint Geometry
    Is the proposed transformation semantically/legalistically available?

Affordance Geometry
    Given an already-valid transformation, how does representation change its cost?
```

CGP-001 stopped in the first region. RIL-001 deliberately begins with an already-earned transformation and asks only the second question.

## 5. Candidate future ladder — NOT CURRENT EVIDENCE

```text
RIL-1  one earned function gets cheaper
RIL-2  a function family gets cheaper under one representation
RIL-3  leverage transfers to held-out functions
RIL-4  leverage has larger effective consequences near a resource boundary
RIL-5  a representation induces surprisingly broad effective generality on constrained substrate
```

Only `RIL-1` has a preregistered assay. RIL-2 through RIL-5 are roadmap items only.

## 6. Next legal scientific action

The next legal action, if explicitly opened, is:

```text
implement RIL-001 exactly from RIL_001_PREREGISTRATION.md
-> freeze implementation
-> audit A_fixed / instrumentation without reading primary cost verdict
-> run preservation checks
-> only then reveal/score the frozen cost result
-> write result and audit
-> STOP
```

Do not modify the RIL-001 scientific contract to make implementation easier or to accommodate an observed result.
