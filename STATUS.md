# Research Status

Last reconciled: **2026-08-26**

Frozen scientific artifacts are append-only. Mutable summaries may reflect later state but may not strengthen, repair, or rewrite frozen claims.

## Corpus Distillation — CLOSED

```text
canonical corpus                         14 repositories
Phase 1 inventory                        COMPLETE
Phase 2 directional substitutability     0 / 182 YES or PARTIAL
L4 full mechanism interfaces             0
L4' bounded evidence/provenance handoff  1
local necessity                          3 NECESSARY / 5 NOT DEMONSTRATED / 1 DISCONFIRMED
common reducible core                    NOT EARNED
minimization                             CLOSED
L5 / L6                                  CLOSED
```

The necessity audit deliberately does not promote recurring themes into necessities without matched failure under violation.

## CGP-001 — CLOSED / NOT EVALUABLE

```text
A_trans            FAIL
failed criteria    8, 9
primary execution  NOT RUN
H_CG               NOT TESTED
```

The null remains a pre-composition semantic/interface typing failure. Criterion 8 rejects retyping an FS target-output label as an NSS resolving-probe identity; criterion 9 rejects treating a local NSS expansion event as constitutive permission for a global FS repair search.

## RIL-001 — CLOSED / POSITIVE

```text
status        REPRESENTATION_INDUCED_LEVERAGE
P1-P9         PASS
C_op R0       9,825,003
C_op R1       4,094,613
Lambda_F^op   2.399495
memory gate   PASS
```

Earned claim: one bounded existence witness for representation-induced computational leverage under the exact frozen FS007 / CPython cost model.

## RIL-002 — CLOSED / FAMILY-WIDE LEVERAGE

```text
family members                 24
P_i                            PASS 24/24
dynamic A_fixed equality       PASS 24/24
Lambda_i^op > 1                PASS 24/24
memory non-regression          PASS 24/24
kappa_F^op                     1.0
full_RIL_coverage              1.0
family status                  FAMILY_WIDE_LEVERAGE
```

Earned claim: bounded family-wide transfer of the unchanged AST→SEM8 representation pair across the exact prospectively frozen 24-member family. This is not provenance-separated generalization.

## RIL-003 — TARGET MANIFEST FROZEN / MEMBER EXECUTION NOT YET AUTHORIZED

Scientific anchors:

```text
preregistration freeze   c5acae018aec09afc9ceece152bb9cdc7a39e112
implementation freeze    f54d9e1a4d8ef35404824d2172ace173af387a96
pre-reveal audit         013435145d7d93985cd056926cfad710dd63e662
entropy target time      2026-08-26T12:00:00.000Z
target-manifest freeze   236763606dc43a31cf99362857c8ae10b1f72c6d
```

Current state:

```text
Q_test                       FROZEN
I_shared whitelist           FROZEN
forbidden target info        FROZEN
R0/R1                        FROZEN: inherited R0_AST / R1_SEM8
implementation               FROZEN
pre-reveal audit             PASS
NIST pulse custody           FROZEN
cryptographic provenance     AUDITED
eligible universe            193
held-out target instances    REVEALED / FROZEN: 24
independent target derivation PASS
RIL_003_TARGET_MANIFEST.json COMMITTED / FROZEN
member preservation          NOT RUN
member opcode/memory         NOT RUN
Lambda vector                DOES NOT EXIST
scientific verdict           NONE
RIL-4                        NOT OPENED
```

The captured NIST Beacon 2.0 pulse is preserved under `experiments/ril_003/custody/`. `RIL_003_CRYPTO_PROVENANCE_AUDIT.md` authenticates the captured entropy source. `RIL_003_TARGET_DERIVATION_AUDIT.md` independently reconstructs the deterministic SHA-256 ranking and the same ordered 24-target family later frozen in `RIL_003_TARGET_MANIFEST.json`.

The preregistered sequence still requires the post-manifest target/provenance execution gate to be closed before member execution. No current artifact records member preservation, opcode/memory results, or a `Lambda` vector.

Next legal transition:

```text
audit committed target manifest / entropy provenance as the execution gate
-> authorize member preservation only if that gate passes
-> run member preservation
-> interpret cost only where A_fixed_i and P_i pass
-> freeze result / final audit
-> STOP
```

No apparatus change, representation adaptation, target redraw, member substitution, or target-specific preprocessing is permitted.

## RD-001 — TERMINAL / AVAILABLE LEVERAGE DEMONSTRATED / TRAINING NOT OPENED

RD-001 separates:

\[
\text{oracle leverage}
\neq
\text{reusable representation}
\neq
\text{learnable representation}.
\]

Frozen positive calibration:

```text
calibration instances                  24
R_STAR native correctness              PASS 24/24
R_STAR designated trap preservation    PASS 24/24
fresh-cost wins R_STAR < R0            PASS 24/24
sum C_fresh(R0)                         2,093,094
sum C_fresh(R_STAR)                     1,134,901
aggregate fresh leverage                1.844296550976693
available leverage                      DEMONSTRATED
```

The implemented oracle is instance-conditioned:

```text
R_STAR(X_i) = R_{J_i}(X_i)
J_i varies across realized instances
```

The preregistered learner output class permits one prospectively frozen reusable global schema. The learner-target compatibility audit therefore terminates:

```text
LEARNER_TARGET_CLASS_MISMATCH
R_L learner specification   NOT FROZEN
Q_TRAIN beacon              NOT CONSUMED
Q_TRAIN                     NOT MATERIALIZED
R_L acquisition             NOT RUN
held-out test lane          NOT OPENED
successor RD assay          NOT OPENED
```

The calibration remains positive evidence for instance-specific oracle leverage. The later compatibility null narrows that evidence; it does not revoke it or convert it into a learner-performance failure.

## Main-line RG-001 — CLOSED AT REALIZER ADMISSION / FUNCTION EQUIVALENCE NOT CONSTITUTED

RG-001 is a parallel `F_LCC` stream and does not modify or consume RIL-003 or RD-001.

### Frozen semantic boundary

```text
F_LCC semantic constitution       FROZEN
case family                       FROZEN: 8 cases
expected semantic labels          NOT HAND-AUTHORED
reference evaluator               IMPLEMENTED
matched C04/C05 death test        PASS
permutation checks                256 / 256 PASS
reference unit tests              6 / 6 PASS
```

The decisive frozen matched pair is:

\[
C04:\mathcal Q(H)=\{\{G,w_a\}\}
\qquad\text{vs}\qquad
C05:\mathcal Q(H)=\{\{G\},\{w_a\}\}.
\]

After withdrawal of `G`'s warrant, `H` contracts in C04 and survives in C05. Pairwise incidence alone aliases the pair.

### Admission anchors

```text
realizer-admission contract freeze  426062c5baa32ea192b99b9a1c8de574eff6eb1e
realizer-admission result freeze    049a2e088c4ab55bf42668607e84f13235e15e18
```

Frozen candidate surfaces:

```text
SSI-CALC v0.1
  repo/commit  bjoern-janson/ssi@362594d4337a1c72556b501b6477ff624db919e1
  checker blob 293d373d13bd68b40ed2e5b0f8754146638981d6

OpenCore Nano V0
  repo/commit  bjoern-janson/opencore@d85aac9fa35ea4ba21afebc73b9cb8970c2a1dbf
  nano.py blob d31dacaf893a58a8280c01704fe666a404c1f56c
```

Both candidates terminate:

```text
A_trans^RG                 FAIL
first failed gate          A6_NATIVE_DEPENDENCY_CONSEQUENCE
specific witness           A8_ALTERNATIVE_ROUTE_NON_ALIASING
semantic-repair gate       A14_NO_SEMANTIC_INVENTION FAIL
terminal                   FUNCTION_EQUIVALENCE_NOT_CONSTITUTED
```

Therefore:

```text
RG-001 preregistration      NOT OPENED
V_F                         NOT RUN
C_F                         NOT RUN
B_F                         NOT RUN
A_F                         NA
adapter repair in RG-001    FORBIDDEN
```

This is a translation/admission null, not `V_F=0` and not a general incapacity claim about either candidate system.

## Cross-program retrospective / principle records

These records sharpen interpretation without reopening any assay:

```text
ADMISSIBILITY_FAILURE_ATLAS.md       retrospective stop-localization audit
COMPRESSION_REVOCABILITY_PRINCIPLE.md compression does not imply correct revocability
REVISION_PROPAGATION_PRINCIPLE.md     revision does not imply operative propagation
```

The atlas reports that, among its 18 mature coded stop events, 14 were localized to constitution/semantics/provenance/identifiability/path failures and 4 to direct effect/calibration nulls. This is a descriptive audit compression, not a population estimate.

## Archived parity RG calibration — TERMINAL / DISAMBIGUATED

A separate branch independently used `RG-001`/`RG-002` for a 12-bit parity calibration. Its frozen artifacts are preserved byte-for-byte under `archive/rg_parity_calibration/`; see `RG_NAMESPACE_NOTE.md`.

```text
parity RG-001  CLOSED / NOT_EVALUABLE
parity RG-002  CLOSED / COST_ROBUSTNESS_TRADEOFF
V0=V1          1
C0             634886
C1             104141
Lambda_C       6.0964077548708
B0             512 / 512
B1             448 / 512
B1 subset B0   proper
```

For that exact calibration only:

\[
C\downarrow\;\not\Rightarrow\;B\uparrow.
\]

It remains calibration evidence only and does not modify main-line RG-001, RIL-003, or RD-001.

## Next legal scientific actions

### RIL-003

```text
close committed-manifest / entropy-provenance execution gate
-> member preservation
-> member cost only where preservation is valid
-> ordered Lambda vector
-> result / final audit
-> STOP
```

### RD

RD-001 is terminal. Any attempt to change the generator from varying `J_i` to a family-wide latent object, widen the learner target class, or design a new acquisition procedure is a new assay and requires a new preregistration.

### Main-line RG

RG-001 v0.1 is terminal at admission. Do not repair it in place and do not open its preregistration retroactively. A successor requires a new identifier and an explicit frozen statement of what changes.
